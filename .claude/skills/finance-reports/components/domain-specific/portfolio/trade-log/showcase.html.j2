{# The `trade-log` showcase -- the VIEW. It receives `d` from
   showcase_controller.py and is the only place a macro is called. Each
   <section> is one state worth seeing. #}
{% extends "_showcase.master.html.j2" %}
{% block content %}
    <section>
      <h3>four trades, three carrying the reasoning behind them</h3>
      {{ c.trade_log(rows=d.rows,
               caption="Trades, FY26 Q2") }}
    </section>
{% endblock %}
