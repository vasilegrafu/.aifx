{# The `earnings-surprise` showcase -- the VIEW. It receives `d` from
   showcase_controller.py and is the only place a macro is called. Each
   <section> is one state worth seeing. #}
{% extends "_showcase.master.html.j2" %}
{% block content %}
    <section>
      <h3>five metrics, three tones — including a miss that is good news</h3>
      {{ c.earnings_surprise(rows=d.rows, period="Q1 FY2026",
               caption="Against consensus") }}
    </section>
{% endblock %}
