{# purpose: who owns the company: top holders, stake, and recent change #}
{# data: summary[] {label, value}   rows[] {holder:str, type:str, shares:str, stake:num, change:str, tone?:str} #}
{% macro ownership_table(rows=[], summary=[], caption="", asof="", note="") %}
  {% if summary %}
  <dl class="fa-ownership-table-summary">
    {% for sm in summary %}
    <div><dt>{{ sm.label }}</dt><dd>{{ sm.value }}</dd></div>
    {% endfor %}
  </dl>
  {% endif %}
  <table class="fin">
  {% if caption or asof %}<caption>{{ caption }}{% if asof %} <span class="fin-asof">as of {{ asof }}</span>{% endif %}</caption>{% endif %}
    <thead><tr><th>Holder</th><th>Type</th><th>Shares</th><th class="fa-ownership-table-stake-head">Stake</th><th>Change</th></tr></thead>
    <tbody>
    {% for r in rows %}
      <tr>
        <th scope="row">{{ r.holder }}</th>
        <td class="fa-ownership-table-type">{{ r.type }}</td>
        <td>{{ r.shares }}</td>
        <td class="fa-ownership-table-stake">
          <span class="fa-ownership-table-track"><span class="fa-ownership-table-fill" data-pct="{{ r.stake }}%"></span></span>
          <span class="fa-ownership-table-pct">{{ r.stake }}%</span>
        </td>
        <td{% if r.get('tone') %} class="tone-{{ r.tone }}"{% endif %}>{{ r.change }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
  {% if note %}<p class="fin-source">{{ note }}</p>{% endif %}
{% endmacro %}
