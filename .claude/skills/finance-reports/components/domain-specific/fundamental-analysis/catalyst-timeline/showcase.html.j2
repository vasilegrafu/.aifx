{# The `catalyst-timeline` showcase -- the VIEW. It receives `d` from
   showcase_controller.py and is the only place a macro is called. Each
   <section> is one state worth seeing. #}
{% extends "_showcase.master.html.j2" %}
{% block content %}
    <section>
      <h3>five catalysts in date order, with likelihood kept separate from direction</h3>
      {{ c.catalyst_timeline(items=d.items) }}
    </section>
{% endblock %}
