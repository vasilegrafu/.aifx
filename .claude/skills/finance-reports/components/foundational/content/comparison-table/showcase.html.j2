{# The `comparison-table` showcase -- the VIEW. It receives `d` from
   showcase_controller.py and is the only place a macro is called. Each
   <section> is one state worth seeing. #}
{% extends "_showcase.master.html.j2" %}
{% block content %}
    <section>
      <h3>marks and free text in one grid</h3>
      {{ c.comparison_table(headers=d.headers, rows=d.rows) }}
    </section>
{% endblock %}
