{# purpose: two-way sensitivity grid: one output across two assumptions, base case marked #}
{# data: cols: str[]   rows[] {row_head:str, cells[] {display:str, tone?:low|mid|high, base?:bool}} #}
{% macro sensitivity_table(cols=[], rows=[], col_label="", row_label="", caption="", note="") %}
  <table class="sensitivity-table">
  {% if caption %}<caption>{{ caption }}</caption>{% endif %}
    <thead>
      <tr>
        <th class="sensitivity-table-corner"><span class="sensitivity-table-row-label">{{ row_label }}</span><span class="sensitivity-table-col-label">{{ col_label }}</span></th>
        {% for c in cols %}<th>{{ c }}</th>{% endfor %}
      </tr>
    </thead>
    <tbody>
    {% for r in rows %}
      <tr>
        <th scope="row">{{ r.row_head }}</th>
        {% for c in r.cells %}
        <td class="sensitivity-table-cell{% if c.get('tone') %} sensitivity-table-{{ c.tone }}{% endif %}{% if c.get('base') %} sensitivity-table-base{% endif %}">{{ c.display }}</td>
        {% endfor %}
      </tr>
    {% endfor %}
    </tbody>
  </table>
  {% if note %}<p class="sensitivity-table-note">{{ note }}</p>{% endif %}
{% endmacro %}
