{# purpose: captioned image with alt text #}
{# data: src: str   alt: str -- REQUIRED, never empty   caption: str #}
{% macro figure(src="", alt="", caption="") %}
  <figure>
    <img src="{{ src }}" alt="{{ alt }}">
    <figcaption>{{ caption }}</figcaption>
  </figure>
{% endmacro %}
