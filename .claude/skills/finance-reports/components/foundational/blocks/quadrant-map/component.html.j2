{# purpose: 2x2 positioning map with items placed by two scores #}
{# data: items[] {label:str, x:0-100, y:0-100, tone?:str}   quadrants: str[4] #}
{% macro quadrant_map(items=[], x_label="", y_label="", quadrants=[], caption="", note="") %}
  <figure class="quadrant-map">
  {% if caption %}<figcaption>{{ caption }}</figcaption>{% endif %}
    <div class="quadrant-map-frame">
      <span class="quadrant-map-y-label">{{ y_label }}</span>
      <div class="quadrant-map-plot">
        {% for q in quadrants %}<span class="quadrant-map-zone quadrant-map-z{{ loop.index }}">{{ q }}</span>{% endfor %}
        {% for it in items %}
        <span class="quadrant-map-item{% if it.get('tone') %} quadrant-map-{{ it.tone }}{% endif %}" data-x="{{ it.x }}%" data-y="{{ it.y }}%">
          <span class="quadrant-map-dot"></span><span class="quadrant-map-label">{{ it.label }}</span>
        </span>
        {% endfor %}
      </div>
      <span class="quadrant-map-x-label">{{ x_label }}</span>
    </div>
    {% if note %}<p class="quadrant-map-note">{{ note }}</p>{% endif %}
  </figure>
{% endmacro %}
