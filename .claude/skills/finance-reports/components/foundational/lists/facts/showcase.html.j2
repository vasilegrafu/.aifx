{# The `facts` showcase -- the VIEW. It receives `d` from
   showcase_controller.py and is the only place a macro is called. Each
   <section> is one state worth seeing. #}
{% extends "_showcase.master.html.j2" %}
{% block content %}
    <section>
      <h3>six facts for a report cover</h3>
      {{ c.facts(rows=d['rows']) }}
    </section>
{% endblock %}
