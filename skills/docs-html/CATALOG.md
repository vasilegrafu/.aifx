# docs-html — catalog

Auto-generated by `python builder.py catalog` — **do not hand-edit**. The one file to read before authoring: every component's call form and every doc-type's purpose, pulled straight from source so it never drifts.

**46 components · 84 doc-types.**

## Components

Call on the `c` namespace inside a doc-type body. Leaves are shown as `{{ c.x(...) }}`; containers that wrap content as `{% call c.x(...) %}…{% endcall %}`.

### structure
*The document skeleton — cover, contents, sections and prose that give every document its shape.*

- `{% call c.lead(text) %}…{% endcall %}` — emphasised executive-summary opening paragraph
- `{% call c.page(heading, variant) %}…{% endcall %}` — one presentation slide (presentation doc-type only)
- `{% call c.prose(text) %}…{% endcall %}` — body-text paragraph wrapper
- `{% call c.section(id, heading) %}…{% endcall %}` — top-level anchored section: heading plus body
- `{% call c.subsection(id, heading) %}…{% endcall %}` — nested subsection: heading plus body
- `{{ c.metadata_header(type_name, title, author, date, version) }}` — document cover: type, title, author, date, version
- `{{ c.toc(entries) }}` — table of contents from (id, label) pairs

### content
*The body set-pieces — code, tables, figures, quotes and collapsibles, the substance a reader came for.*

- `{% call c.code(lang) %}…{% endcall %}` — plain preformatted code
- `{% call c.code_block(path, lang) %}…{% endcall %}` — framed code with title bar and runtime syntax coloring
- `{% call c.collapsible(summary) %}…{% endcall %}` — click-to-expand details block
- `{% call c.quote(text, source) %}…{% endcall %}` — pull quote with attribution
- `{{ c.comparison_table(headers, rows) }}` — feature yes/no/partial comparison grid
- `{{ c.figure(src, alt, caption) }}` — captioned image with alt text
- `{{ c.table(headers, rows, wide) }}` — basic data table; wide=true to scroll horizontally

### lists
*Enumerations — parallel facts, ordered steps, checklists and traceable identifiers.*

- `{{ c.bullets(items) }}` — unordered list for parallel, unranked points
- `{{ c.checklist(items) }}` — status checklist: done/pending/blocked
- `{{ c.facts(rows) }}` — key/value definition list
- `{{ c.numbered(items) }}` — ordered list where sequence matters
- `{{ c.steps(items) }}` — numbered procedure steps
- `{{ c.trace_id(id) }}` — anchorable traceable identifier

### callouts
*Attention blocks that interrupt the flow to flag something the reader must not miss.*

- `{% call c.callout(type) %}…{% endcall %}` — attention block: note/warning/decision/risk
- `{% call c.todo_marker(text) %}…{% endcall %}` — inline unresolved-todo flag

### blocks
*Specialist record blocks — requirements, acceptance criteria, KPIs, timelines, meters and risk.*

- `{% call c.revision_note(rev) %}…{% endcall %}` — inline dated note about a change
- `{{ c.acceptance_criteria(id, title, given, when, then) }}` — Given/When/Then acceptance test for a requirement
- `{{ c.approval_block(rows) }}` — sign-off table of names and roles
- `{{ c.change_history(rows) }}` — versioned revision log for the cover
- `{{ c.glossary(terms) }}` — defined-terms list
- `{{ c.kpi_tiles(tiles) }}` — row of headline metric tiles with trend
- `{{ c.meter(label, value, max, display) }}` — labeled progress/target bar
- `{{ c.requirement(id, title, priority, label, description, fit) }}` — numbered requirement card with priority and fit criterion
- `{{ c.risk_matrix(items) }}` — 5x5 probability-by-impact heat grid of risks
- `{{ c.timeline(items) }}` — dated milestone timeline with status

### business
*Deal and finance artifacts — statements, journal entries, scenario and SWOT analysis, verdict badges and party blocks.*

- `{{ c.badge(label, variant) }}` — inline status/verdict pill
- `{{ c.financial_table(headers, rows, caption) }}` — statement table with subtotal/total rows and negatives
- `{{ c.journal_entry(id, date, memo, lines) }}` — double-entry accounting posting (debit/credit)
- `{{ c.party_block(from_title, from_lines, to_title, to_lines) }}` — from/to parties block for contracts and invoices
- `{{ c.pros_cons(pros, cons) }}` — two-column pros vs cons list
- `{{ c.scenarios(items) }}` — bull/base/bear scenario cards
- `{{ c.swot_grid(strengths, weaknesses, opportunities, threats) }}` — 2x2 strengths/weaknesses/opportunities/threats grid

### front-back-matter
*The scholarly apparatus — references, appendices and footnotes that frame the body.*

- `{% call c.appendices() %}…{% endcall %}` — wrapper isolating appendix lettering from body numbering
- `{% call c.appendix(id, heading) %}…{% endcall %}` — one lettered appendix section
- `{{ c.footnotes(notes) }}` — numbered footnotes list with back-links
- `{{ c.references(items) }}` — numbered, anchorable reference list

### diagrams
*Interactive Mermaid diagrams — pan, zoom, fit, fullscreen, a live editor and SVG export, all at view time.*

- `{% call c.chart_echarts(option, height) %}…{% endcall %}` — declarative data chart (bar/line/pie/scatter/candlestick…) via Apache ECharts, view-time SVG
- `{% call c.diagram_mermaid(code) %}…{% endcall %}` — interactive Mermaid diagram: pan, zoom, live editor

### math
*LaTeX mathematics — plain source in the document, rendered by KaTeX at view time with a readable fallback.*

- `{% call c.formula(tex) %}…{% endcall %}` — LaTeX math block, rendered by KaTeX at view time

## Doc-types

Compose with `python builder.py new <type> "<title>"`.

### general
*Cross-domain business and project documents — charters, plans, proposals, policies, reports and records used in any field.*

- `business-case` — justification for an investment or initiative
- `change-request` — proposed change with impact and approval
- `decision-record` — dated record of one decision and its context (ADR-style)
- `feasibility-study` — assesses whether an option is viable
- `incident-postmortem` — blameless analysis of an incident and follow-ups
- `meeting-minutes` — record of a meeting: attendees, decisions, actions
- `policy` — binding rules and responsibilities on a topic
- `presentation` — slide deck (presentation layout)
- `project-charter` — authorises a project: scope, objectives, stakeholders
- `project-management-plan` — how a project will be executed and controlled
- `proposal` — offer to do work, with scope, approach and price
- `risk-register` — tracked list of risks with owners and responses
- `roadmap` — time-phased plan of milestones and deliverables
- `service-level-agreement` — committed service levels and remedies
- `standard-operating-procedure` — step-by-step procedure for a routine task
- `statement-of-work` — contractual scope, deliverables and terms of an engagement
- `status-report` — periodic progress update: done, next, risks
- `user-guide` — end-user instructions for a product

### software
*The software delivery lifecycle — requirements, architecture, design, test, release and operations documents.*

- `api-specification` — endpoints, contracts and schemas of an API
- `architecture-decision-record` — one architectural decision, dated and immutable
- `coding-standards` — conventions and rules for writing code
- `data-migration-plan` — plan to move data between systems safely
- `database-design-document` — schema, entities and relationships of a database
- `defect-report` — reproducible bug report with severity
- `deployment-runbook` — step-by-step deployment procedure
- `developer-setup-guide` — how to set up the local dev environment
- `disaster-recovery-plan` — how to restore service after a disaster (RTO/RPO)
- `operations-runbook` — operational procedures for running a service
- `performance-test-report` — load/performance test results vs targets
- `product-requirements-document` — what a product must do and why
- `project-closure` — formal close-out: outcomes, lessons, sign-off
- `release-notes` — what changed in a release, for users
- `requirements-traceability-matrix` — maps requirements to design, code and tests
- `rollback-plan` — how to revert a change if it fails
- `software-architecture` — high-level structure, components and decisions of a system
- `software-design-document` — detailed design of a module or system
- `software-requirements-specification` — complete functional and non-functional requirements (ISO 29148)
- `sprint-retrospective` — what went well/badly and improvements for next sprint
- `technical-specification` — technical design and implementation detail of a feature
- `test-case-specification` — detailed test cases with steps and expected results
- `test-plan` — scope, approach and schedule of testing
- `test-summary-report` — results and verdict of a test cycle
- `threat-model` — assets, threats and mitigations for a system (STRIDE-style)
- `use-case-specification` — actor goals and interaction flows
- `user-interface-design-specification` — UI screens, states and interaction rules
- `user-story-backlog` — prioritised list of user stories

### finance
*Corporate and personal finance planning and reporting — budgets, forecasts, valuations and net-worth statements.*

- `budget` — planned income and expenditure over a period
- `cash-flow-forecast` — projected cash inflows and outflows
- `management-report` — periodic financial/operational report for management
- `net-worth-statement` — assets minus liabilities at a point in time
- `valuation-report` — estimated value of an asset or business, with method

### investing
*Investment research and portfolio management — theses, due diligence, backtests, journals and reviews.*

- `backtest-report` — historical performance of a strategy with metrics
- `due-diligence-report` — structured investigation of an investment target
- `earnings-note` — analysis of a company's earnings release
- `investment-policy-statement` — objectives, constraints and allocation rules for a portfolio
- `investment-thesis` — argued case for buying/holding an investment
- `market-outlook` — forward view of markets and positioning
- `portfolio-review` — periodic review of holdings and performance
- `strategy-specification` — definition of a trading/investment strategy and its rules
- `trade-journal` — log of trades with rationale and outcome
- `watchlist` — monitored securities with triggers and notes

### accounting
*Bookkeeping and financial-statement documents — invoices, credit notes, expense reports, reconciliations and statements.*

- `credit-note` — refund/adjustment against a prior invoice
- `expense-report` — itemised employee expense claim for reimbursement
- `financial-statements` — income statement, balance sheet and cash flow set
- `invoice` — billing document requesting payment for goods/services
- `reconciliation-report` — matches two records (e.g. ledger vs bank) and explains differences

### research
*Scientific and analytical write-ups — research and data-analysis reports, literature reviews, experiment logs and white papers.*

- `data-analysis-report` — methods, results and interpretation of a data analysis
- `experiment-log` — chronological record of experiment runs and results
- `literature-review` — survey and synthesis of prior work
- `research-report` — full research write-up: question, method, findings
- `white-paper` — authoritative report advocating a position or solution

### economics
*Macro and sector analysis — economic and industry analysis and policy briefs.*

- `economic-analysis` — analysis of an economy or macro trend
- `industry-analysis` — structure, forces and outlook of an industry
- `policy-brief` — concise argument and recommendation for a policy decision

### engineering
*Physical and systems engineering records — specifications, calculations, inspections, maintenance, failure and risk analysis.*

- `bill-of-materials` — hierarchical parts/materials list with quantities
- `commissioning-report` — record that a system was tested and handed over
- `design-calculation-note` — documented engineering calculation with assumptions
- `equipment-specification` — technical requirements for a piece of equipment
- `failure-analysis` — root-cause investigation of a failure
- `inspection-report` — findings from an inspection against criteria
- `maintenance-plan` — scheduled maintenance tasks and intervals
- `risk-assessment` — identifies hazards and rates/controls their risk

### tools
*Utility documents for working with the design system itself, such as the diagram editor scratchpad.*

- `diagram-editor` — single-diagram scratchpad for drafting a Mermaid diagram

### fallback
*The catch-all when no specific type fits — a generic, lightly-structured document.*

- `generic-document` — unstructured fallback when no specific type fits
