{# purpose: the load-bearing arguments of a thesis: claim, evidence, and what would break it #}
{# data: items[] {claim:str, evidence?:str, falsifier?:str} #}
{% macro thesis_pillars(items=[]) %}
  <ol class="investing-pillars">
  {% for it in items %}
    <li class="investing-pillar">
      <p class="investing-pillar-claim">{{ it.claim }}</p>
      {% if it.get('evidence') %}<p class="investing-pillar-evidence">{{ it.evidence }}</p>{% endif %}
      {% if it.get('falsifier') %}<p class="investing-pillar-falsifier"><span class="investing-pillar-tag">Breaks if</span> {{ it.falsifier }}</p>{% endif %}
    </li>
  {% endfor %}
  </ol>
{% endmacro %}
