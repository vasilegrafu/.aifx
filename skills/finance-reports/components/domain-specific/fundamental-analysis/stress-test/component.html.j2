{# purpose: portfolio or company impact under named stress scenarios #}
{# data: rows[] {scenario:str, trigger:str, impact:str, magnitude:num, tone:str, response:str} — magnitude % for the bar #}
{% macro stress_test(rows=[], caption="", note="") %}
  <table class="investing-fin investing-stress">
  {% if caption %}<caption>{{ caption }}</caption>{% endif %}
    <thead><tr><th>Scenario</th><th>Trigger</th><th class="investing-stress-impact-head">Impact</th><th>Response</th></tr></thead>
    <tbody>
    {% for r in rows %}
      <tr>
        <th scope="row">{{ r.scenario }}</th>
        <td class="investing-stress-trigger">{{ r.trigger }}</td>
        <td class="investing-stress-impact">
          <span class="investing-stress-value investing-tone-{{ r.tone }}">{{ r.impact }}</span>
          <span class="investing-stress-track"><span class="investing-stress-fill investing-stress-fill-{{ r.tone }}" data-pct="{{ r.magnitude }}%"></span></span>
        </td>
        <td class="investing-stress-response">{{ r.response }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
  {% if note %}<p class="investing-st-source">{{ note }}</p>{% endif %}
{% endmacro %}
