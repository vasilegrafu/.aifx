{# purpose: who owns the company: top holders, stake, and recent change #}
{# data: summary[] {label, value}   rows[] {holder:str, type:str, shares:str, stake:num, change:str, tone?:str} #}
{% macro ownership_table(rows=[], summary=[], caption="", asof="", note="") %}
  {% if summary %}
  <dl class="investing-own-summary">
    {% for sm in summary %}
    <div><dt>{{ sm.label }}</dt><dd>{{ sm.value }}</dd></div>
    {% endfor %}
  </dl>
  {% endif %}
  <table class="investing-fin ownership">
  {% if caption or asof %}<caption>{{ caption }}{% if asof %} <span class="investing-macro-asof">as of {{ asof }}</span>{% endif %}</caption>{% endif %}
    <thead><tr><th>Holder</th><th>Type</th><th>Shares</th><th class="investing-own-stake-head">Stake</th><th>Change</th></tr></thead>
    <tbody>
    {% for r in rows %}
      <tr>
        <th scope="row">{{ r.holder }}</th>
        <td class="investing-own-type">{{ r.type }}</td>
        <td>{{ r.shares }}</td>
        <td class="investing-own-stake">
          <span class="investing-own-track"><span class="investing-own-fill" data-pct="{{ r.stake }}%"></span></span>
          <span class="investing-own-pct">{{ r.stake }}%</span>
        </td>
        <td{% if r.get('tone') %} class="investing-tone-{{ r.tone }}"{% endif %}>{{ r.change }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
  {% if note %}<p class="investing-st-source">{{ note }}</p>{% endif %}
{% endmacro %}
